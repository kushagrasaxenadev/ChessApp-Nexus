# Server-only secrets. Configure hosted values in the Sites control plane.
OPENAI_API_KEY=

# Keep model selection configurable so fast/deep/fallback routes can evolve.
AI_MODEL_FAST=
AI_MODEL_DEEP=
AI_MODEL_FALLBACK=

# Optional dedicated services used after the foundation milestone.
ENGINE_SERVICE_URL=
REALTIME_SERVICE_URL=
